kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  namespace: default
  name: dockercoins
rules:
- apiGroups: ["apps"]
  resources: ["deployments, services"]
  verbs: ["get", "list"]
  resourceNames: []